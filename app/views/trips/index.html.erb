<div class="container">
  <h1>Trips Index</h1>
  <ul>
    <% @trips.each do |trip| %>
      <li style="list-style: none">
        <div class="card m-2 p-2">
         <h2>Projeto: <%= trip.project.name %></h2>
          <p>Data: <%= trip.date %></p>
          <p>Usuário: <%= trip.user.first_name  %></p>
        </div>
      </li>
    <% end %>
  </ul>
</div>


<%# && trip.project.name == current_user. bla bla && trip.project.date == current_user %>
